<UserControl x:Class="_335thUserCapture.View.CaptureOneUserOnComputer.LocationSavedAt"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock TextWrapping="Wrap">The backup file will be saved at the following location</TextBlock>
        <TextBox IsReadOnly="True" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}" />
        <!-- Disallowing user to select backup location
        <Button Grid.Column="1" Grid.Row="2" Content="Browse" Click="OpenBrowse" Height="21" VerticalAlignment="Bottom"/>      
        -->
    </Grid>
</UserControl>
